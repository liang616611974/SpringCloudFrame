version: "2"
services:
  eureka:
    image: springcloud/spring-cloud-demo-web-eureka
    ports:
    - "8761:8761"
    environment:
    - "spring.profiles.active=test"
    hostname: test
  config:
    image: cloud_test/config
    ports:
    - "8888:8888"
    hostname: config